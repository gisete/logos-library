import React from 'react';

const Header = () => {
    return(
        <nav className="navbar navbar-light text-center">
            <h1 className="header-logo h3">Logos Library</h1>
        </nav>
    )
}

export default Header;